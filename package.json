{
  "name": "faber",
  "version": "0.1.0",
  "description": "Spec-Driven Development CLI — specs → plan → tasks → test → code",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "faber": "src/index.ts"
  },
  "files": [
    "src/",
    "templates/",
    "scripts/"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "dev": "bun src/index.ts"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.2.4",
    "@stryker-mutator/core": "^9.5.1",
    "@stryker-mutator/vitest-runner": "^9.5.1",
    "@types/bun": "latest",
    "@types/node": "^25.3.1",
    "@types/yauzl-promise": "^4.0.1",
    "@vitest/coverage-v8": "4",
    "fast-check": "^4.5.3",
    "vitest": "4"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "neverthrow": "^8.2.0",
    "yaml": "^2.8.2",
    "yauzl-promise": "^4.0.0"
  }
}
